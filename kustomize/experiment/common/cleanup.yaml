apiVersion: iter8.tools/v1alpha3
kind: Experiment
metadata:
  name: sklearn-iris-experiment-VERSION
spec:
    cleanupJobSpec:
        template:
            spec:
              containers:
              - name: cleanup
                image: iter8-kubectl
                command: 
                    - $PATH_TO_KUSTOMIZE_FOLDER/scripts/promote.sh
                env:
                    - name: PATH_TO_KUSTOMIZE_FOLDER
                      value: "/tekton/created/workspace"
              restartPolicy: Never
            backoffLimit: 4